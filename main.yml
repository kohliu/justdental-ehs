server:
  applicationConnectors:
    - type: http
      port: 8080
    - type: https
      port: 8081
      keyStorePath: C:\Users\ujjwa\cacerts
      keyStorePassword: justdental
      validateCerts: false
  adminConnectors:
    - type: http
      port: 8082
#    - type: https
#      port: 8083
#      keyStorePath: C:\Users\ujjwa\example2.jks
#      keyStorePassword: example2
#      validateCerts: false
  rootPath: /api/*
  applicationContextPath: /


# Database settings.
database:
    # the name of the JDBC driver, mysql in our case
    driverClass: com.mysql.jdbc.Driver
    # the username
    user: root
    # the password
    password: root
    # the JDBC URL; the database is called hello_world
    url: jdbc:mysql://localhost:3306/jdehs_dev
    # the SQL query to run when validating a connection's liveness
    validationQuery: "/* MyService Health Check */ SELECT 1"

    properties:
        hibernate.show_sql: true
        hibernate.generate_statistics: false
        hibernate.hbm2ddl.auto: validate # validates schema when service is started

swagger:
  resourcePackage: com.techstomach.ehs.resources